import {auth,onAuthStateChanged,sendPasswordResetEmail,signInWithEmailAndPassword} from './firebase-web.js';
const form=document.querySelector('[data-login-form]');const message=document.querySelector('[data-auth-message]');const next=new URLSearchParams(location.search).get('next')||'account.html';let submitted=false;
const show=(text,success=false)=>{message.textContent=text;message.classList.toggle('success',success)};
onAuthStateChanged(auth,user=>{if(user&&!submitted)location.replace(next)});
form.addEventListener('submit',async event=>{event.preventDefault();show('');const button=form.querySelector('[type="submit"]');button.disabled=true;button.textContent='Signing in...';submitted=true;try{await signInWithEmailAndPassword(auth,form.email.value.trim(),form.password.value);location.replace(next)}catch(error){submitted=false;show(error.code==='auth/invalid-credential'?'Incorrect email or password.':error.code==='auth/invalid-email'?'Enter a valid email address.':'We could not sign you in. Please try again.')}finally{button.disabled=false;button.textContent='Sign in'}});
document.querySelector('[data-auth-reset]').addEventListener('click',async()=>{const email=form.email.value.trim();if(!email)return show('Enter your email address first.');try{await sendPasswordResetEmail(auth,email);show('Password reset email sent.',true)}catch{show('Could not send the reset email.')}});
