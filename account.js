import {auth,db,doc,getDoc,onAuthStateChanged,signOut} from './firebase-web.js';
const view=document.querySelector('[data-profile-view]'),loading=document.querySelector('[data-account-loading]');const esc=value=>{const n=document.createElement('span');n.textContent=value||'';return n.innerHTML};const initials=name=>(name||'Elevate User').split(/\s+/).slice(0,2).map(p=>p[0]).join('').toUpperCase();
document.querySelector('[data-sign-out]').addEventListener('click',async()=>{await signOut(auth);location.replace('login.html')});
async function show(user){const [profileSnap,dataSnap]=await Promise.all([getDoc(doc(db,'users',user.uid)).catch(()=>null),getDoc(doc(db,'userdata',user.uid)).catch(()=>null)]);const profile=profileSnap?.data?.()||{},cloud=dataSnap?.data?.()||{},name=profile.name||profile.displayName||user.displayName||user.email.split('@')[0],summary=cloud.summary||{};document.querySelector('[data-profile-name]').textContent=`Welcome, ${name}`;document.querySelector('[data-profile-email]').textContent=user.email;document.querySelector('[data-side-name]').textContent=name;document.querySelector('[data-side-team]').textContent=profile.organizationName||profile.teamName||(profile.mode==='organization'?'Team workspace':'Personal workspace');document.querySelector('[data-avatar]').textContent=initials(name);document.querySelector('[data-song-count]').textContent=summary.songCount||0;document.querySelector('[data-library-count]').textContent=summary.libraryCount||0;document.querySelector('[data-playlist-count]').textContent=summary.playlistCount||0;document.querySelector('[data-recent-songs]').innerHTML=summary.recentSongs?.length?summary.recentSongs.map((song,index)=>`<article><strong>${esc(song.title)}</strong><span>${String(index+1).padStart(2,'0')}</span></article>`).join(''):'<p>No cloud songs yet. Open ElevateFlow and edit your library to sync.</p>';const banner=document.querySelector('[data-sync-banner]');banner.querySelector('strong').textContent=dataSnap?.exists()?'Cloud library connected':'Cloud library is ready';banner.querySelector('span').textContent=dataSnap?.exists()?'Your lightweight Flow data is available on this account.':'Your first Flow edit will appear here automatically.';const date=cloud.updatedAt?.toDate?.();document.querySelector('[data-sync-time]').textContent=date?`Updated ${new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(date)}`:'';loading.hidden=true;view.hidden=false}
onAuthStateChanged(auth,user=>{if(!user)return location.replace('login.html?next=account.html');show(user).catch(()=>{loading.querySelector('strong').textContent='Account could not load';loading.querySelector('p').textContent='Check your connection and refresh this page.'})});
